# Core dependencies
openai>=1.0.0
python-dotenv>=1.0.0
loguru>=0.7.0
rich>=13.0.0
prompt_toolkit>=3.0.0
requests>=2.31.0          # used by web_search / fetch_url (internet.py)
aiohttp>=3.9.0            # async AppSec scanner engine
h2>=4.1.0                 # HTTP/2 race-condition scanner
httpx[http2]>=0.27.0      # HTTP/2 smuggling probes
pycryptodome>=3.20.0      # AES payload obfuscation

# Vector memory (optional but recommended)
chromadb>=0.4.0
sentence-transformers>=2.0.0  # semantic search embeddings for ChromaDB RAG

# Additional utilities
pydantic>=2.0.0
boto3>=1.28.0
fpdf2>=2.7.0
matplotlib>=3.7.0
Pillow>=10.0.0              # terminal_screenshot PNG rendering

# PDF report rendering (install either for direct PDF output; HTML fallback works without both)
# weasyprint>=61.0      # best quality; needs libpango/cairo system libs
xhtml2pdf>=0.2.11       # pure-Python fallback

playwright>=1.48.0
pyee>=11.1.0          # required by playwright (event emitter)
selenium>=4.20.0

webdriver-manager>=4.0.0
git-dumper>=1.0.6

pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0
requests-mock>=1.11.0

networkx>=3.0
